import Lookup from '~/components/lookup';

const Home = () => (
    <div>
        <h1 className='text-lg text-red-700 font-bold'>Home</h1>
        <main>
            <Lookup />
        </main>
    </div>
);

export default Home;
